import { PipeFilterOrdersPipe } from './pipe-filter-orders.pipe';

describe('PipeFilterOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFilterOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
